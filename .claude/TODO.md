# TODO.md — Backlog du projet

> Légende des statuts :
> `[ ]` À faire | `[~]` En cours | `[x]` Terminé | `[!]` Bloqué

---

## 🔴 Priorité haute

### Backend
- [x] Brancher `AuditorAware<String>` (`@EnableJpaAuditing` déjà actif sur `StoreApplication`, mais sans `AuditorAware` les `createdBy`/`updatedBy` resteront `null`)
- [x] `SecurityConfig` + filtre JWT + `UserDetailsService` — chaîne complète : `JwtService`, `JwtAuthenticationFilter`, `UserDetailsServiceImpl`, `JwtProperties`, enum `Claim`
- [x] Use case d'inscription propriétaire : `Account` + `Proprietaire` + `Entreprise` + premier `Magasin` + abonnement d'essai
- [x] Endpoints `/auth/register`, `/auth/login`, `/auth/refresh`, `/auth/logout`
- [x] Migration Flyway (passer `ddl-auto` à `validate`) — `spring-boot-starter-flyway` 4.0.6 + `V1__init_schema.sql` manuel (40 tables, JOINED), `baseline-on-migrate=true` `baseline-version=1`
- [x] Activer CORS pour autoriser `http://localhost:3000` (frontend dev) — fait via `CorsOriginFilter` dans `security/config/`
- [x] **Création d'employés** (`POST /api/v1/employees`) — règles data-driven (propriétaire = tout, manager = tout sauf manager, un seul rôle élevé par magasin), `EmployeServiceImpl` + `EmployeController` + tests
- [x] **CRUD Magasin** (2026-05-12) — `findAll(Pageable)`, `findResponseById`, `activate`, `deactivate`, `updateMine`, soft delete. Scoping OWNER vs MANAGER. `@Query` projection en text block.
- [x] **CRUD Entreprise** (2026-05-12) — ADMIN sur `POST/GET/PATCH /entreprises`, PROPRIETAIRE sur `/me`. Convention `SELECT new EntrepriseResponse(e)` adoptée.
- [x] **Strategy pattern `UserPrincipalContextStrategy`** (2026-05-12) — élimination `instanceof` dans `UserPrincipalFactoryImpl`. Package `security/application/strategies/` : `UserPrincipalContext`, interface + 3 impls (Proprietaire/Employe/Utilisateur fallback). Dispatch "most-specific wins".
- [x] **Extraction `UserResponse` DTO** (2026-05-12) — `AccountResponse` agrège un `UserResponse` au lieu des 5 champs à plat. Constructeur `UserResponse(Utilisateur)`.
- [x] **Seed ERP** (2026-05-12) — 4 rôles (PROPRIETAIRE, MANAGER, VENDEUR, ADMIN) + 79 permissions (4 anciennes + 75 nouvelles MODULE_ACTION) selon matrice PDF `Roles Permissions Erp Saas.pdf`. MANAGER absorbe Magasinier+Comptable. `DataInitializer` étendu (idempotent).
- [x] **RBAC YAML synchronisable** (2026-05-13) — Externalisation rôles+permissions dans `src/main/resources/security/roles-permissions.yml`. `RbacProperties` (`security.rbac.sync` + `security.rbac.file`). `IRolesPermissionsSyncService` (additif + log des orphelins, jamais de suppression). `DataInitializer` allégé : appel conditionnel à `sync()` si `sync=true`. 4 tests unitaires (création, idempotence, mise à jour assoc, détection orphelins). i18n exceptions (`RbacConfigException` + 4 clés FR/EN). Surcharge `LocalizedRuntimeException(messageKey, cause, args)` pour préserver la stacktrace racine.
- [x] **Logs HTTP entrants/sortants + requestId MDC** (2026-05-13) — `HttpRequestLoggingFilter` (`OncePerRequestFilter` + `ContentCachingRequest/ResponseWrapper`) : log INFO entrée (`→ METHOD path from ip`) + sortie (`← status METHOD path in Xms | request: {...} | response: {...}`). Masquage auto des champs sensibles (`password`, `accessToken`, `refreshToken`, `secret`, `token` → `"***"`). Tronquage 2 KB. Skip `/actuator/**`, `/swagger-ui/**`, `/v3/api-docs/**` + content-types binaires. `requestId` UUID dans MDC propagé via `logging.pattern.console`. `X-Forwarded-For` privilégié. 6 tests unitaires (ListAppender Logback).
- [x] **GlobalException WARN/ERROR** (2026-05-13) — handlers 4xx → `logger.warn(...)` concis ; handlers 5xx → `logger.error(..., cause)` avec stack. Helper `buildError(message, status, throwable)` surchargé.
- [x] **Convention "documenter services applicatifs"** (2026-05-13, règle 29 `ARCHITECTURE.md`) — javadoc concise (1 phrase) sur classe + chaque méthode des `<X>ServiceImpl`, **aucun commentaire inline**. Modèle : `RolesPermissionsSyncServiceImpl`.
- [x] **Pré-requis Produit/Catégorie/Qualité** (2026-05-13) — FK `@ManyToOne Entreprise` ajoutée sur `CategoryProduct` + `Quality`. Migration Flyway `V4__add_entreprise_to_category_quality.sql`. Enum `PermissionCode` enrichi (12 valeurs : CATEGORY_PRODUCT_*, QUALITY_*, PRODUCT_*). YAML `roles-permissions.yml` aligné (ADMIN/PROPRIETAIRE/MANAGER = CRUD complet, VENDEUR = READ seul). Clés i18n FR/EN (notFound, alreadyExists, notOwned).
- [x] **CRUD CategoryProduct** (2026-05-13) — DTOs Request/Response (avec constructeur entity), domain service enrichi (`create`, queries scopées par entreprise + unicité libellé), `ICategoryProductService` + impl avec scoping `ICurrentUserService` (unicité libellé skippée si inchangé en update), controller `/api/v1/category-products` (POST/GET paginé/GET id/PUT/DELETE) avec `@PreAuthorize` granulaires. 14 tests service + 6 tests controller, 182 / 182 verts.
- [x] **CRUD Quality** (2026-05-13) — Pattern décalqué sur `CategoryProduct`. DTOs, domain service enrichi, `IQualityService` + impl avec scoping et unicité libellé, `QualityController` (`/api/v1/qualities`) avec permissions granulaires `QUALITY_*`. 14 tests service + 6 tests controller, 202 / 202 verts.
- [x] **CRUD Product** (2026-05-13) — DTOs avec sous-DTOs imbriqués `CategoryProductResponse` + `QualityResponse` (règle 23), `ProductDomainService.create(request, category, quality, entreprise)`, `IProductService` + impl `@Transactional(readOnly=true)` au niveau classe, vérif cohérence category & quality appartiennent à l'entreprise du caller (via `ensureBelongsToCurrentEntreprise` cross-services), unicité `reference` par entreprise, controller `/api/v1/products` avec `@PreAuthorize('PRODUCT_*')`. 15 tests service + 7 tests controller, 224 / 224 verts. Upload d'images reporté.
- [x] **`Product.imagePrincipal`** (2026-05-13) — relation `@OneToOne PieceJointe` indépendante de `images` (galerie). Migration Flyway V5 (colonne `image_principal_id` + FK + index). `ProductResponse` expose `imagePrincipalId` (id seul, pas le blob). `ProductRequest` inchangé. Endpoint upload reporté (permission `PRODUCT_UPLOAD_IMAGE` existante en YAML). +2 tests, 226 / 226 verts.
- [x] **Service `UploadFile` + endpoints upload/delete image principale** (2026-05-13) — `IUploadFileService` + `UploadFileServiceImpl` (`common/service`) : valide MIME `image/{jpeg,png,webp,gif}` + non-vide, construit `PieceJointe` (blob + `date=now`). Config `spring.servlet.multipart.max-file-size=5MB`. `ProductDomainService.setImagePrincipal(Product, PieceJointe)`. Endpoints `PUT /api/v1/products/{id}/image` (multipart `@RequestPart("file")`) + `DELETE /api/v1/products/{id}/image`, permission `PRODUCT_UPLOAD_IMAGE`. orphanRemoval supprime l'ancienne `PieceJointe` lors d'un remplacement / clear. 6 tests `UploadFileServiceImplTest` + 4 service + 2 controller, 238 / 238 verts.
- [x] **Upload multiple — galerie `Product.images`** (2026-05-13) — `IUploadFileService.buildImages(List<MultipartFile>) → List<PieceJointe>` (réutilise `buildImage` en boucle, stop au 1er invalide). `ProductDomainService.addImages(Product, List<PieceJointe>)` (append cumulatif). `IProductService.uploadImages(UUID, List<MultipartFile>) → List<UUID>` (ids des images créées). Endpoint `POST /api/v1/products/{id}/images` (multipart `@RequestPart("files")`) avec permission `PRODUCT_UPLOAD_IMAGE`, status 201. `ProductResponse` inchangé (galerie exposée via futur endpoint dédié). 3 tests UploadFile + 2 service + 1 controller, 244 / 244 verts.
- [x] **Visualisation et suppression d'image** (2026-05-13) — DTO `ImageDownloadResponse(content, contentType)` dans `common/dto`. `ProductDomainService.findImageInProduct(...)` + `removeImage(...)`. `IProductService.getImagePrincipal(UUID)` / `getImage(productId, imageId)` / `deleteImage(productId, imageId)`. Endpoints `GET /products/{id}/image` (permission `PRODUCT_READ`), `GET /products/{id}/images/{imageId}` (`PRODUCT_READ`), `DELETE /products/{id}/images/{imageId}` (`PRODUCT_UPLOAD_IMAGE`). 8 tests service + 3 controller.
- [x] **`PieceJointe.contentType` stocké** (2026-05-13) — remplace la détection magic bytes par un champ `String contentType NOT NULL` sur l'entité `PieceJointe`. Migration Flyway V6. `UploadFileServiceImpl.buildImage` stocke `file.getContentType()` (lowercased) à l'upload. `IProductService.getImagePrincipal/getImage` retournent `pieceJointe.getContentType()` directement. Suppression de `IUploadFileService.detectImageContentType` et de ses 5 tests. 255 / 255 verts. Bénéfice : extensible à tout MIME (PDF, docs) sans modif code.
- [x] **Listing galerie produit** (2026-05-13) — `GET /api/v1/products/{id}/images` (permission `PRODUCT_READ`) retourne `List<ImageMetadataResponse>` avec `(id, date, contentType, url)` pour chaque image. `url` = path relatif `/api/v1/products/{productId}/images/{imageId}` que le frontend peut utiliser directement dans `<img src={img.url}>`. DTO dans `produit/application/dto` (path produit dans l'URL). `IProductService.listImages(UUID)` scopé par entreprise. 3 tests service + 1 controller, 259 / 259 verts.
- [x] **Sous-DTOs Summary catégorie/qualité + URL image dans `ProductResponse`** (2026-05-13) — `CategoryProductSummaryResponse(id, libelle)` et `QualitySummaryResponse(id, libelle)` remplacent les sous-DTOs CRUD complets dans `ProductResponse`. Champ `image` (URL relative `/api/v1/products/{id}/image`) ajouté, `imagePrincipalId` retiré (l'URL suffit). Front a tout pour afficher + éditer.
- [x] **Module stock — fonctionnalité 1 : CRUD Fournisseur** (2026-05-13) — FK `@ManyToOne Entreprise` ajoutée sur `Fournisseur` (migration Flyway V7, NOT NULL direct). Enum `PermissionCode` + YAML enrichis : `SUPPLIER_{CRUD}` (4 nouvelles, ADMIN/PROPRIETAIRE/MANAGER = CRUD, VENDEUR = READ). i18n FR/EN. DTOs `FournisseurRequest`/`FournisseurResponse(Fournisseur)`. `FournisseurDomainService` enrichi (queries scopées + unicité reference). `IFournisseurService` + impl avec scoping `ICurrentUserService`, unicité `reference` par entreprise (skippée si null/blank). Controller `/api/v1/suppliers` avec `@PreAuthorize('SUPPLIER_*')`. 13 tests service + 7 controller, 279 / 279 verts.
- [x] **Module stock — fonctionnalité 2 : CRUD ProductFournisseur** (2026-05-13) — entité enrichie de `referenceFournisseur` + `origine` (+ promotion `product_id`/`fournisseur_id` en NOT NULL), migration Flyway V8. Sous-DTOs `ProductSummaryResponse(id, nom, reference)` + `FournisseurSummaryResponse(id, nom)` ajoutés. DTOs `ProductFournisseurRequest(productId, fournisseurId, prixAchat, refFournisseur?, origine?)` avec validations + `ProductFournisseurResponse(ProductFournisseur)` avec sous-DTOs imbriqués. `ProductFournisseurDomainService` enrichi (projection JPQL avec scoping via `product.entreprise.id`, query par produit, unicité paire). `IProductFournisseurService` + impl `@Transactional(readOnly=true)` : scoping cross-entity via `IProductService.ensureBelongsToCurrentEntreprise` + `IFournisseurService.ensureBelongsToCurrentEntreprise`, unicité `(product, fournisseur)`, update ne modifie que prix/refFournisseur/origine (FK immuables). Controller `/api/v1/product-suppliers` avec `?productId=` (filtre optionnel) et `@PreAuthorize('SUPPLIER_*')`. i18n FR/EN `productFournisseur.{notFound,alreadyExists,notOwned}`. 13 tests service + 8 controller, 300 / 300 verts.
- [x] **Module stock — fonctionnalité 3 : Entrée stock manuelle** (2026-05-14) — `POST /api/v1/stocks/entries`. Crée `EntreeStock` (lot FIFO) + upsert `Stock` (moyenne pondérée prix d'achat) + journalise `MouvementStock(ENTREE_ACHAT)`. Migration V9 (drop `Stock.productFournisseur` + UNIQUE `(magasin_id, produit_id)`). Permission `STOCK_ENTRY`. 13 tests, 313 verts.
- [x] **Module stock — fonctionnalité 4 : Consultation stock** (2026-05-14) — `GET /api/v1/stocks?magasinId=&productId=&page=&size=` paginé + `GET /api/v1/stocks/{id}`. Pattern record `StockFilter` validé par `ValidatorService` (règle de codage 30 — max 3 params). Permission `STOCK_READ`. 12 tests, 325 verts.
- [x] **Module stock — fonctionnalité 5 : Consultation mouvements** (2026-05-14) — `GET /api/v1/stock-movements?magasinId=&stockId=&productId=&type=&startDate=&endDate=&page=&size=`. Record `MouvementStockFilter` avec `@EnumValue`, `@DatePattern`, méthodes utilitaires (`typeAsEnum`, `fromDateTime`). Sous-DTO `MouvementDetailResponse`. Helpers transverses `common/tools/` (`DateHelper`, `EnumHelper`, `UuidHelper`). 12 tests, 337 verts.
- [x] **Module stock — fonctionnalité 6 : Sortie stock FIFO** (2026-05-14, service interne) — `ISortieStockService.create(...)` (pas d'endpoint REST exposé : sera consommé par le futur module Vente). Boucle FIFO sur `EntreeStock` triés par `createdAt ASC`, crée 1 `SortieStock` par lot consommé (avec marge `(prixVente − prixAchat) × qty`), décrémente `Stock`, journalise `MouvementStock(SORTIE_VENTE)`. Erreurs : `EntityException("stock.notFound")`, `BadArgumentException("stock.exit.insufficientQuantity")`. 6 tests service, 331 verts.
- [x] **Module stock — fonctionnalité 7 : Ajustement stock manuel** (2026-05-14) — `POST /api/v1/stocks/adjustments` (permission `STOCK_ADJUSTMENT`). Types `POSITIF`/`NEGATIF`, motifs typés (`RETROUVAILLE`, `PERTE`, `CASSE`, `VOL`, `ERREUR_INVENTAIRE`, `AUTRE`). Positif = mini `EntreeStock` avec fournisseur obligatoire ; négatif = FIFO sans `SortieStock`. Validation cohérence motif/type. MANAGER conserve la permission (responsable du magasin — fraude tracée via `MouvementStock.createdBy`). 13 tests, 344 verts.
- [x] **Module stock — fonctionnalité 8 : Seuils d'approvisionnement** (2026-05-14) — `PATCH /api/v1/stocks/{id}/threshold` (permission `STOCK_ADJUSTMENT`) + `GET /api/v1/stocks/below-threshold?magasinId=` (permission `STOCK_READ`). DTO `StockThresholdRequest(@Min(0) seuilApprovisionnement)`. Query JPQL `WHERE qty <= seuil AND seuil > 0`. 6 tests, 350 verts.
- [x] **Module stock — fonctionnalité 9 : Valorisation stock** (2026-05-14) — `GET /api/v1/stocks/valuation?magasinId=`. Retourne `StockValuationResponse(magasinId, valeurTotale = SUM(qty × prixAchatMoyen), nombreLignes)`. Permission `STOCK_READ`. 2 tests, 352 verts.
- [x] **Module stock — fonctionnalité 10 : Reporting marges** (2026-05-14) — `GET /api/v1/reports/margins?magasinId=&productId=&fournisseurId=&startDate=&endDate=` (permission `REPORT_STOCK`). Service dédié `IMarginReportService`. Query JPQL sur `SortieStock` JOIN `EntreeStock` pour agrégation `SUM(marge)`, `SUM(quantiteSortie)`, `COUNT`. Filtres optionnels via SpEL `:#{#filter.X}`. 3 tests, 355 verts.
- [x] **Module stock — fonctionnalité 11 : Lots expirants** (2026-05-14) — `GET /api/v1/stocks/expiring-lots?magasinId=&productId=&daysAhead=N&page=&size=`. Service dédié `IExpiringLotsService`. Filter `ExpiringLotsFilter` avec méthode utilitaire `untilDate() = today.plusDays(daysAhead)`. Query JPQL filtre `WHERE dateExpiration <= :until AND qtyRestante > 0 ORDER BY dateExpiration ASC`. Response avec sous-DTOs Summary magasin/produit/fournisseur. Permission `STOCK_READ`. 1 test, 356 verts.
- [x] **Module achat F12-F14 — commande + facture + paiements atomique** (2026-05-14 soirée) — `POST /api/v1/purchases` (permission `PURCHASE_CREATE`) orchestre dans une seule transaction : `CommandeAchat` (référence auto via `ReferenceHelper.generate("CMD")`) + `FactureAchat` (numéro unique par entreprise) + N lignes produits + entrée stock immédiate par ligne (réutilise `IEntreeStockService`) + paiement initial éventuel. Sous-services `ICommandeAchatService`, `IFactureAchatService`, `IPaiementAchatService`. Listings dédiés `GET /purchases/orders`, `GET /purchases/invoices`, `GET /purchases/invoices/echeances`. Permissions `PURCHASE_{CREATE,READ}` + `PAYMENT_{CREATE,READ}`. 5 records `<X>Create` (regroupement >3 params, règle 30). Helper transverse `ReferenceHelper`. 28 tests, 384 verts.
- [x] **Module dépense — CRUD CategoryDepense + CRUD Depense** (2026-05-14 soirée) — `CategoryDepense` scopée entreprise (FK + unicité `(entreprise_id, nom)`), `Depense` scopée magasin (libelle, dateDepense, montant, modePaiement, category). Endpoints `/api/v1/expense-categories` + `/api/v1/depenses` (listing paginé filtré + `/total` agrégé). Migration V10 (FK entreprise sur category_depense + mode_paiement NOT NULL sur depense + drop date_echeance). `DepenseFilter` record validé par ValidatorService. Permissions `EXPENSE_CATEGORY_{CRUD}` + `EXPENSE_{CRUD}`. 15 tests, **392 verts**.
- [x] **Module vente — fonctionnalité 1 : CRUD Client** (2026-05-15) — `Client extends Person` rattaché à `Magasin`. Scoping double via `IMagasinService.ensureAccessibleByCurrentUser` (employé = magasin, propriétaire = entreprise). Endpoints `/api/v1/clients` (POST/GET avec `?nom=&prenom=&page=&size=`/GET id/PUT/DELETE). DTOs `ClientRequest`/`ClientResponse(Client)` (sans magasinId/entrepriseId — scoping invisible côté API)/`ClientSummaryResponse(id, nomComplet)`/`ClientFilter(nom, prenom, page, size)`. Permissions `CLIENT_{CRUD}` ajoutées (ADMIN/PROPRIETAIRE/MANAGER = CRUD complet, VENDEUR = CREATE+READ). Pas de migration (table déjà en V1). Décision projet : "client anonyme" = `CommandeVente.clientId` simplement nullable plus tard, pas d'enregistrement dédié. 3 nouvelles règles : variables explicites, DTO Filter ≥ 2 critères, recherche nom/prénom séparés. 15 tests service + 9 controller, **415 verts**.
- [x] **Module vente — fonctionnalité 3 : Vente atomique** (2026-05-16) — `POST /api/v1/ventes` (permission `SALE_CREATE`) orchestre dans une seule transaction : `CommandeVente` (référence auto `VTE-yyyyMMdd-HHmmssSSS` via `ReferenceHelper`) + lignes par `ProductFournisseur` (vente par variante PF, pas par Product) + sorties stock FIFO consommées par PF (filtre `EntreeStock.productFournisseur`) liées à `LigneCommandeVente` via FK `SortieStock.ligneVente` + `FactureClient` (numéro auto `FAC-VTE-...`) + `PaiementVente` initial éventuel. Décisions métier : `prixUnitaire ≥ pf.prixVente` (plancher PF), vendeur EMPLOYE obligatoire (PROPRIETAIRE → 403), client nullable (vente anonyme), 1 ligne = 1 PF (le client choisit la variante, mix de variantes = N lignes). Détails : `GET /api/v1/ventes/{commandeId}` (permission `SALE_READ`) avec sous-DTOs `UserSummaryResponse(id, nomComplet)` du vendeur résolu via `createdBy`, lignes + paiements. Migrations V12 (`paiement_vente` + `ligne_commande_vente.product_fournisseur_id`) + V13 (drop `commande_vente.vendeur_id` — Option Minimaliste, audit `createdBy` fait foi). Refactor `UserPrincipal` : ajout `userId` (= Utilisateur.id métier) + rename `userId` → `accountId` + claim JWT `USER` (24 tests adaptés cross-modules). Nouveaux records `CommandeVenteCreate`/`LigneCommandeVenteCreate`/`FactureClientCreate`/`SortieStockCreate`/`SortieStockForVente`/`VenteContext`. Nouveaux services : `IVenteService` + `IEmployeService.findCurrentUser()` (throw Forbidden si pas Employe) + `IAccountService.findUserSummaryByAccountId()` (résolution `createdBy`). i18n FR/EN (4 clés). 9 nouveaux tests, **441 / 441 verts** (décompte agrégé Surefire). **Revue + durcissement (2026-05-16 PM)** : 20 défauts catalogués et corrigés sur 9 commits atomiques — D1 V14 NOT NULL `product_fournisseur_id`, D2-D10 factorisation `NameHelper.formatNomComplet` + `UuidHelper.parseOptional`, D5-D7 extraire `getMontantPaye()` répétés, D11 `applyMontantPaye` dans le domain service, D13 extraire `quantite`/`prixUnitaire`, D14 unifier `consumeLotsFifo`/`consumeLotsFifoForVente` via `LotConsumptionContext`, D15-D17 +9 tests manquants, D18 `dateVente` optionnelle `@PastOrPresent` (defaut today), D20 V15 rename `dateEcheache → dateEcheance` + `@NotNull @FutureOrPresent` (saisi par vendeur), D19 `datePaiement` optionnel `@PastOrPresent` + nouveau record `PaiementVenteCreate`. Suite finale **448 / 448 verts**.
- [x] **Module vente — fonctionnalité 4 : Listings paginés filtrés (commandes vente + factures client + paiements par facture)** (2026-05-16 soirée) — 5 endpoints en projection JPQL pure (règle 24), tous scopés entreprise dans la WHERE de la query (sécurité multi-tenant sans risque de fuite). **Listings sans `user` (vendeur), résolution `user` seulement sur le détail** via 1 query unique JPQL (`LEFT JOIN Account ON CAST(a.id AS string) = c.createdBy + LEFT JOIN a.user u + TRIM(CONCAT(...))`) — économise N queries d'audit par page. 2 constructeurs `CommandeVenteResponse` ajoutés : `(CommandeVente)` listing et `(CommandeVente, UUID, String)` détail. Endpoints livrés : `GET /api/v1/commandes-vente` (paginé : magasinId obligatoire, clientId/dates optionnels), `GET /api/v1/commandes-vente/{id}` (détail avec user), `GET /api/v1/factures-client` (paginé : statut + clientId + dates), `GET /api/v1/factures-client/{id}` (détail), `GET /api/v1/factures-client/{id}/paiements` (paginé via `Pageable` Spring Data). Records `CommandeVenteFilter` + `FactureClientFilter`. Nouveaux services applicatifs : `ICommandeVenteService`, `IFactureClientService`, `IPaiementVenteService`. i18n FR/EN : `commandeVente.notFound`, `factureClient.notFound`. Pas de migration BDD. **465 / 465 verts** (+17 vs 448 pré-F-V4). **Itération (2026-05-16 nuit)** : `CommandeVenteFilter` enrichi de 5 critères supplémentaires (`vendeurId` via `LEFT JOIN Account a ON CAST(a.id AS string) = c.createdBy + a.user.id`, `statut` via `@EnumValue`, `reference` LIKE insensitive, `montantMin`/`montantMax`) pour répondre à "recherche par date / client / vendeur / montant / statut / référence". Retrait simultané de la saisie de `dateVente` du `VenteRequest` (force `LocalDate.now()` en dur côté service, plus de backdate API). 2 commits atomiques : `82a4b99` (enrichissement filter) + `c5fab9f` (retrait dateVente). **466 / 466 verts**.
- [x] **Module vente — fonctionnalité 5 : Paiement échelonné** (2026-05-16 soirée tardive) — `POST /api/v1/factures-client/{id}/paiements` (permission `SALE_PAY`, déjà en YAML). Permet d'ajouter un paiement sur une facture existante (crédit client en plusieurs fois). Body `PaiementVenteRequest` (existant). Symétrie avec `POST /api/v1/factures-achat/{id}/paiements`. Validations publiques (règle 27) : `ensureBelongsToCurrentEntreprise`, `ensureNotAlreadyPaid` (statut ≠ PAYEE), `ensureAmountDoesNotExceedRemaining` (`montant + montantPaye ≤ montantTotal`). Logique : crée `PaiementVente` via `PaiementVenteCreate` + `FactureClientDomainService.applyPaiement` (recalcule statut auto). **Plus de propagation sur commande** depuis le refactor V16. i18n FR/EN : `factureClient.alreadyPaid`, `paiementVente.exceedsRemainingAmount`. 5 tests service + 2 controller. **481 / 481 verts** (+7).
- [x] **Refactor V16 — supprimer la redondance montantTotal/montantPaye sur CommandeVente** (2026-05-16 soirée tardive) — Constat soulevé en planification F-V5 : `CommandeVente.montantTotal/montantPaye` dupliquent `FactureClient.montantTotal/montantPaye`. Décision : refactor d'abord (option A). Migration V16 (`DROP COLUMN` montant_total + montant_paye). Entité `CommandeVente` allégée. `CommandeVenteResponse` reçoit les montants en arguments explicites via 4 constructeurs (au lieu de getter sur entité). Queries JPQL repos : `LEFT JOIN FactureClient f ON f.commande = c` pour projeter `f.montantTotal/f.montantPaye`. Filtre `montantMin/Max` migré de `c.montantTotal` à `f.montantTotal`. `sumMontantTotalByMagasinAndDay` déplacé de `CommandeVenteRepository` vers `FactureClientRepository`. `CommandeVenteDomainService` perd `applyMontantTotal`, `applyMontantPaye`, `sumMontantCommandesForCaisse`. `FactureClientDomainService` gagne `sumMontantCommandesForCaisse`. `CaisseServiceImpl` injecte `FactureClientDomainService`. `VenteServiceImpl.applyPremierPaiementIfPresent` simplifiée (paramètre `commande` retiré). 10 fichiers modifiés, aucun test cassé. **474 / 474 verts**.
- [x] **Module vente — Top N produits les plus vendus par jour** (2026-05-16 soirée tardive) — `GET /api/v1/ventes/caisse/top-produits?magasinId=&date?&nombre?` (permission `SALE_READ`). `nombre` défaut 3, `date` défaut today. Tri par **quantité totale vendue** (pas par CA — décision utilisateur). 1 query JPQL agrégée avec `GROUP BY p.id, p.nom, p.reference` + `ORDER BY SUM(l.quantite) DESC` + limit via `Pageable`. Réponse `List<TopProduitResponse(productId, nom, reference, quantiteVendue, chiffreAffaires)>`. Record `TopProduitsFilter` avec accesseurs `effectiveDate()` (today si null), `startOfDay/endOfDay`, `toPageable`. Méthode ajoutée à `ICaisseService` + endpoint dans `CaisseController`. 2 tests service + 2 controller. **474 / 474 verts**.
- [x] **Module vente — résumé caisse journalier** (2026-05-16 fin de soirée) — `GET /api/v1/ventes/caisse/resume?magasinId=&date=YYYY-MM-DD` (permission `SALE_READ`). Retourne `CaisseResumeResponse(magasinId, date, nombreCommandes, nombreProduits, totalCommandes, totalPaiements)`. **Stratégie : 4 queries JPQL scalaires** agrégées (`COUNT(c)`, `COALESCE(SUM(c.montantTotal), 0)`, `COALESCE(SUM(l.quantite), 0)` via JOIN sur ligne, `COALESCE(SUM(p.montant), 0)` sur PaiementVente). Filtre `createdAt BETWEEN startOfDay AND endOfDay`. **`nombreProduits` = somme des quantités vendues** (pas nombre de lignes) — décision utilisateur. **`totalPaiements` = tous les paiements créés ce jour-là** (sémantique tiroir-caisse, inclut paiements échelonnés sur ventes antérieures). Multi-tenant scoping `entreprise.id` dans chaque query + `IMagasinService.ensureAccessibleByCurrentUser` en amont. Record `CaisseResumeFilter` + `ICaisseService` + `CaisseController` (dans `vente/presentation/`). Pas de migration BDD, pas de nouvelle permission. 3 tests service + 1 controller. **470 / 470 verts** (+4 vs 466).
- [x] **Module vente — fonctionnalité 2 : Recherche produit vendeur + déplacement quality vers PF + prixVente** (2026-05-15 soirée) — Migration V11 : déplace `quality_id` de `product` vers `product_fournisseur` (backfill depuis product avant DROP, drop FK dynamique pour BD baselinée), ajoute `prix_vente NUMERIC(19,2) NOT NULL` sur `product_fournisseur` et `ligne_commande_achat` (backfill = prix_achat), UNIQUE `(product_id, fournisseur_id, quality_id)`. Validation métier `prixVente > prixAchat` (strict). Nouveau service dédié `IProductSearchService` (cassé du cycle PF/Stock/Product). Endpoint `GET /api/v1/products/search?q=&magasinId=&page=&size=` (PRODUCT_READ, magasinId obligatoire PROPRIETAIRE / implicite EMPLOYE, retourne `Page<ProductSearchResponse>` avec sous-liste `ProductFournisseurStockResponse(id, quality, fournisseur, prixVente, quantiteEnStock)` agrégée par PF, 2 queries pour éviter N+1). Endpoint `PUT /api/v1/product-suppliers/{id}/prix-vente` (SUPPLIER_UPDATE, manager peut ajuster librement). `AchatServiceImpl` met à jour `pf.prixVente = ligne.prixVente` à chaque achat. **424 / 425 verts** (1 test contextLoads KO à cause d'un nettoyage Flyway BD à exécuter manuellement).

### Frontend
- [ ] Vérifier l'install de TanStack React Query (clé `"root": "github:tanstack/react-query"` dans `package.json` est suspecte — l'import `@tanstack/react-query` fonctionne‑t‑il vraiment ?)
- [ ] Customiser `src/app/layout.tsx` : metadata projet (titre/description), `lang="fr"`
- [ ] Créer `src/lib/api/client.ts` (instance axios + intercepteur JWT)
- [ ] Brancher `QueryClientProvider` dans un `Providers` client‑component
- [ ] Pages auth `(auth)/login` et `(auth)/register` (formulaire RHF + Zod, appel API)
- [ ] Layout authentifié `(dashboard)/layout.tsx` (sidebar + header + garde de session)

---

## 🟡 Priorité normale

### Backend
- [x] **Annulation de vente / retour** (2026-05-18) — `POST /api/v1/ventes/{id}/annuler` (permission `SALE_CANCEL` réservée ADMIN/PROPRIETAIRE/MANAGER, retirée du VENDEUR). Ré-injecte le stock FIFO consommé : recrédite `EntreeStock.quantiteRestante`, marque chaque `SortieStock` liée d'un flag `annulee=true` (audit préservé, marges et caisse filtrent les annulées), journalise 1 `MouvementStock(RETOUR_CLIENT)` par lot recrédité, incrémente `Stock.quantite`. Bascule `CommandeVente.statut=ANNULEE` (+ motif enum `MotifAnnulationVente{ERREUR_SAISIE, REFUS_CLIENT, ARTICLE_DEFECTUEUX, AUTRE} + commentaire libre`) et `FactureClient.statut=ANNULEE` (paiements conservés tels quels pour audit, remboursement hors-app). Fenêtre temporelle configurable via `SaleProperties.cancelWindowHours` (défaut 24h, env `SALE_CANCEL_WINDOW_HOURS`, règle 38). 6 queries Caisse/Margin adaptées pour exclure `commande.statut = ANNULEE` (sumQuantite + countCommandes + ventilationVendeur + sumMontantTotal + sumMontantPaiement + topProduits) + `MarginReport` filtre `sortie.annulee = false`. Migration V22 (3 cols `commande_vente` + 1 col `sortie_stock` + index). 5 tests service + 3 controller. **711 / 711 verts** (+8 vs 703).
- [x] **Édition/suppression ligne d'achat + refactor DRAFT/VALIDATE** (2026-05-18) — Refactor structurel : `POST /api/v1/achats` crée désormais une commande en `DRAFT` (lignes + traçabilité lot persistées, **pas** de stock ni facture). 3 nouveaux endpoints : `PUT /achats/orders/{commandeId}/lignes/{ligneId}` (perm `PURCHASE_UPDATE`, garde `ensureCommandeIsDraft`), `DELETE` idem (refus si dernière ligne), `POST /achats/{commandeId}/validate` (perm `PURCHASE_APPROVE`) qui matérialise : facture + entrées stock + journal `ENTREE_ACHAT` + update `pf.prixVente` + bascule statut `RECEPTIONNEE`. Migration V23 (`ligne_commande_achat +numero_lot +date_expiration` — la traçabilité lot doit être persistée entre DRAFT et VALIDATE, plus juste un transit `LigneAchatRequest → EntreeStockCreate`). DTOs : `AchatRequest` allégé (sans facture), nouveaux `AchatDraftResponse`/`AchatValidateRequest`/`LigneAchatUpdateRequest`. Validations publiques `ensureCommandeIsDraft`/`ensureLigneBelongsToCommande`/`ensureNotLastLigne`. 13 tests service + 8 controller. **725 / 725 verts** (+14 vs 711).
- [x] **Édition/suppression ligne de vente + refactor DRAFT/VALIDATE** (2026-05-18) — Symétrique au refactor achat : `POST /api/v1/ventes` crée désormais en `DRAFT` (lignes + validations PF + prix plancher, **sans** consommation stock ni facture). 3 nouveaux endpoints : `PUT/DELETE /ventes/orders/{commandeId}/lignes/{ligneId}` (perm `SALE_UPDATE`/`SALE_DELETE`, gardes DRAFT/belongs/notLast) et `POST /ventes/{commandeId}/validate` (perm **`SALE_APPROVE`** créée et attribuée à VENDEUR/MANAGER/PROPRIETAIRE/ADMIN) qui matérialise : consomme stock FIFO + crée FactureClient + paiement initial éventuel + bascule statut `DELIVERED`. `VenteRequest` allégé (sans `dateEcheance` ni `premierPaiement` — déplacés dans `VenteValidateRequest`). Pas de migration BDD (la traçabilité lot n'est pas pertinente côté vente). **6 queries Caisse passées de `<> ANNULEE` à `= DELIVERED`** (exclut DRAFT + ANNULEE, sémantique tiroir-caisse stricte). DTOs nouveaux : `VenteDraftResponse`/`VenteValidateRequest`/`LigneVenteUpdateRequest`. `VenteContext` supprimé (orphelin). Cleanup `VenteServiceImpl` : méthodes `createLignesAndProcessStock`/`processVenteLine` remplacées par `persistLignes` + `consumeStockForLigne`. **739 / 739 verts** (+14 vs 725).
- [x] **Annulation d'achat** (2026-05-18) — `POST /api/v1/achats/{id}/annuler` (permission `PURCHASE_CANCEL` créée et attribuée à ADMIN/PROPRIETAIRE/MANAGER, volontairement absente de VENDEUR). Symétrique à l'annulation de vente avec une **garde métier supplémentaire** : refuse l'annulation si au moins un lot a déjà été partiellement ou totalement consommé par une vente (`quantiteRestante < quantiteInitiale`). Si tous les lots sont intacts : pour chaque `EntreeStock` issu de la commande → décrément `Stock.quantite` de la quantité restante, flag `EntreeStock.annulee=true` (audit historique préservé, `quantiteRestante` volontairement inchangée), 1 `MouvementStock(RETOUR_FOURNISSEUR)` par lot avec ref commande. Bascule `CommandeAchat.statut=ANNULEE` (+ motif enum `MotifAnnulationAchat{ERREUR_SAISIE, REFUS_FOURNISSEUR, ARTICLE_DEFECTUEUX, AUTRE}` + commentaire libre) et `FactureAchat.statut=ANNULEE` (paiements conservés tels quels pour audit, remboursement hors-app). Fenêtre temporelle configurable via `PurchaseProperties.cancelWindowHours` (défaut 24h, env `PURCHASE_CANCEL_WINDOW_HOURS`, règle 38). 5 queries adaptées pour exclure les lots annulés (`EntreeStockRepository` ×4 : FIFO + FIFO par PF + lots expirants + lots actifs ; `ProductRepository.searchByEntrepriseWithActiveLots`). `Stock` agrégé (valorisation + below-threshold) déjà décrémenté par cancel → aucun ajustement nécessaire. `MarginReport` non impacté (filtre déjà `sortie.annulee=false`). Migration V24 (`commande_achat +motif/commentaire/date_annulation`, `entree_stock +annulee` + index). 6 tests service + 3 controller. **750 / 750 verts** (+11 vs 739).
- [x] **Réception partielle commande achat** (2026-05-18) — Refactor structurel séparant validation comptable et réception physique. `validate` (DRAFT → **VALIDEE**) ne crée plus que la facture (montants gelés), sans aucun `EntreeStock` ni mouvement stock ni update `pf.prixVente`. Nouvel endpoint `POST /api/v1/achats/{id}/receptions` (perm `PURCHASE_APPROVE`) qui prend `ReceptionAchatRequest(List<LigneReceptionRequest(ligneId, quantite, numeroLot?, dateExpiration?)>)` : pour chaque ligne reçue → crée un `EntreeStock` (lot du request en priorité, sinon snapshot ligne), upsert `Stock` agrégé, journalise `MouvementStock(ENTREE_ACHAT)`, update `pf.prixVente`, incrémente `LigneCommandeAchat.quantiteRecue`. Bascule auto en `RECEPTIONNEE` quand toutes les lignes sont totalement reçues (sum `quantiteRecue == quantite`), sinon **`PARTIELLEMENT_RECEPTIONNEE`** (nouveau statut). Validations publiques `ensureReceivable` (VALIDEE ou PARTIELLEMENT_RECEPTIONNEE), `ensureLignesDistinctes` (anti double-comptage par réception), `ensureQuantiteRecueNotExceeded`. **Cancel élargi aux statuts VALIDEE et PARTIELLEMENT_RECEPTIONNEE** : VALIDEE = juste annule facture (pas de lot à retirer), PARTIELLEMENT_RECEPTIONNEE = retire les lots créés avec mêmes garde-fous `ensureNoLotConsumed`. Message i18n `notReceptionnee` renommé en `notCancellable` (plus précis). Migration V25 (`ligne_commande_achat +quantite_recue INT NOT NULL DEFAULT 0`). 4 clés i18n FR/EN `commandeAchat.receive.*`. 11 tests service (10 receive + 2 cancel additionnels VALIDEE/PARTIELLEMENT_RECEPTIONNEE) + 3 tests controller (200 / 400 lignes vides / 400 quantite 0). **765 / 765 verts** (+15 vs 750).
- [x] **Cache Redis L1 — référentiels par entreprise** (2026-05-18) — Étend l'intégration Redis à 4 référentiels métier scopés entreprise : `categories-product-by-entreprise` (TTL 1h), `qualities-by-entreprise` (TTL 1h), `fournisseurs-by-entreprise` (TTL 30 min), `expense-categories-by-entreprise` (TTL 1h). Constantes + named caches dans `RedisCacheConfig`. Nouveau bean `EntrepriseScopedKeyGenerator` (`org.store.config`) qui préfixe chaque clé par `entrepriseId` (résolu via `ICurrentUserService`) + nom de méthode + params — évite les fuites cross-tenant. Annotations `@Cacheable(keyGenerator="entrepriseScopedKeyGenerator")` sur `findAllByCurrentEntreprise(Pageable)` de chaque service + `@CacheEvict(allEntries=true)` sur create/update/delete (12 méthodes au total sur 4 services : CategoryProductServiceImpl, QualityServiceImpl, FournisseurServiceImpl, CategoryDepenseServiceImpl). 764/764 verts (NoOp cache en test).
- [x] **Intégration Redis — cache catalogue public** (2026-05-18) — Pose les fondations cache applicatif avec Redis. Dépendances Maven `spring-boot-starter-data-redis` (Lettuce) + `spring-boot-starter-cache`. Config externalisée dans `src/main/resources/redis.yml` (importée via `spring.config.import: classpath:redis.yml`), env vars `REDIS_HOST`/`REDIS_PORT`/`REDIS_PASSWORD`/`REDIS_DATABASE`/`SPRING_CACHE_TYPE`. `RedisCacheConfig` (`@EnableCaching` + `@ConditionalOnProperty(spring.cache.type=redis)`) : bean `RedisCacheManager` avec sérialisation JSON Jackson (`JavaTimeModule` + default typing `NON_FINAL` via `BasicPolymorphicTypeValidator`), TTL par défaut 10 min, named cache `"public-catalog"` 5 min. `docker-compose.yml` racine (postgres:15-alpine + redis:7-alpine, healthchecks, volumes nommés). Premier cas d'usage : `PublicCatalogServiceImpl.findCatalog` annoté `@Cacheable(PUBLIC_CATALOG)` + `@CacheEvict(allEntries=true)` sur **15 méthodes CUD** des 3 services admin (PlanAbonnementServiceImpl, PromotionServiceImpl, SubscriptionTypeServiceImpl, chacun avec create/update/activate/deactivate/delete). En test : surefire system property `spring.cache.type=none` désactive le cache → NoOpCacheManager auto-fourni par Spring Boot, tests existants inchangés. Pas de `@SpringBootTest` ajouté (test du cache nécessitera Testcontainers plus tard). 764/764 verts (hors `StoreApplicationTests.contextLoads` pré-cassé indépendamment de Redis — BDD locale non démarrée).
- [ ] **CRUD Magasin complet** — Magasin a un domain service mais peu d'endpoints exposés. Compléter : listing paginé filtré, GET by id, PUT update, activate/deactivate. Symétrique au CRUD employé qu'on vient de livrer.
- [ ] **Statistiques par magasin** — `GET /api/v1/magasins/{id}/stats` : nombre employés, clients, produits en stock, valeur totale stock, CA mensuel. Composition au-dessus de plusieurs services (users + vente + stock).
- [ ] **Reporting / dashboard** — Endpoints agrégés pour dashboard : CA cumulé sur période, marge globale, top clients, stock dormant, factures en retard. Pas de UI, juste les API. Compose au-dessus des modules vente/stock/facture existants.
- [ ] **Transferts inter-magasins** — Permission `STOCK_TRANSFER` existe déjà dans le YAML mais aucun endpoint. `POST /api/v1/stocks/transfers` : sortir d'un magasin source vers magasin destination, deux mouvements stock symétriques (`TRANSFER_OUT` + `TRANSFER_IN`). Conservation des lots PF + prix d'achat moyen.
- [ ] **Audit log / historique** — Endpoint `GET /api/v1/audit-logs` pour parcourir les actions sensibles (création/suppression employé, ajustements stock, annulations). Filtre par utilisateur, période, type d'action. Aujourd'hui audit dispersé dans `createdBy`/`updatedBy` — soit on construit une vue dédiée, soit on introduit une table `audit_log` avec entrée par action.
- [ ] **Mot de passe oublié (reset par email)** — `POST /auth/forgot-password` (envoie token au mail) + `POST /auth/reset-password?token=...&newPassword=...`. Sans authentification, par token temporaire. Necessite la mise en place de la couche notifications (EMAIL).
- [ ] **Génération PDF facture vente** — `GET /api/v1/factures-client/{id}/pdf` : facture imprimable PDF avec entête entreprise (logo, raison sociale, NINEA/RCCM, adresse) + lignes + totaux + paiements. Bibliothèque OpenPDF ou iText.
- [ ] **Hard delete avec garde-fous** — `DELETE` réel sur employe ET magasin avec verification d'historique dépendant (commandes, mouvements stock, ventes, factures). Sinon 406. Complément au soft delete actuel pour nettoyage d'erreurs de saisie irrécupérables.
- [ ] **Sessions JWT actives + révocation** — `GET /me/sessions` liste les sessions actives + `POST /me/sessions/{id}/revoke`. Stockage des refresh tokens en BDD avec timestamp last-used. Permet de "déconnecter" un device perdu / volé.
- [ ] **Historique d'achat d'un client** — `GET /api/v1/clients/{id}/ventes` : toutes les commandes vente d'un client donné, paginées, avec filtres (période, statut, montant). Utile pour connaître le profil d'achat d'un client récurrent.
- [ ] **Listing produits global enrichi** — Ajouter à `GET /products` des filtres : par catégorie, par qualité, par seuil prix, par disponibilité en stock. Aujourd'hui le listing est basique (pagination seule).
- [ ] **Top vendeurs (par CA)** — `GET /api/v1/ventes/caisse/top-vendeurs?magasinId=&from=&to=&nombre=N` : classe les vendeurs par CA cumulé sur la période. Complément au top-produits déjà livré.
- [ ] **Suppression ligne d'inventaire en BILAN** — Aujourd'hui les lignes sont figées au BILAN (rapport produit). Permettre la suppression d'une ligne en BILAN si on détecte une erreur avant clôture, avec régénération du rapport. Affine la correction post-bilan.

#### Module abonnement (10 points — squelette riche en entités, pas de REST exposé)

État actuel : 7 entités (`Abonnement`, `PlanAbonnement`, `TypeAbonnement`, `PaiementAbonnement`, `Promotion`, `Coupon`, `UtilisationCoupon`), 7 domain services minimaux, 2 services applicatifs internes (`createTrial`, `findFirstTrialActif`), 0 controller, 0 permission `ABONNEMENT_*`/`PLAN_*` dans le YAML. Seul le flow trial est branché à l'inscription propriétaire.

- [x] **CRUD PlanAbonnement** (ADMIN) (2026-05-17) — `/api/v1/plans` POST/GET filter (nom/actif/visible/trial)/GET id/PUT/PATCH activate-deactivate/DELETE. DTOs Request (13 champs)/Response/SummaryResponse/Filter. Permissions `PLAN_{CRUD}`. 13 tests service + 9 controller. **591 verts**.
- [x] **CRUD TypeAbonnement** (ADMIN) (2026-05-17) — `/api/v1/subscription-types` même pattern. Permissions `SUBSCRIPTION_TYPE_{CRUD}`. Validation cohérence `reductionType`+`valeurReduction` (POURCENTAGE ≤ 100). 14 tests service + 8 controller. **613 verts**.
- [x] **CRUD Coupon** (ADMIN) (2026-05-17) — `/api/v1/coupons` même pattern + sous-DTO `PlanAbonnementSummaryResponse`. Unicité `code`, validation fenêtre temporelle + réduction. Permissions `COUPON_{CRUD}`. 12 tests service + 8 controller. **633 verts**.
- [x] **CRUD Promotion** (ADMIN) (2026-05-17) — `/api/v1/promotions` même pattern + sous-DTO plan. Validation fenêtre temporelle + réduction. Permissions `PROMOTION_{CRUD}`. 12 tests service + 7 controller. **641 verts**.
- [x] **Toggle renouvellement auto** (PROPRIETAIRE) (2026-05-17) — `PATCH /api/v1/abonnements/{id}/renouvellement-auto` (permission `SUBSCRIPTION_UPDATE`). DTO `RenouvellementAutoRequest(boolean)`. Le propriétaire bascule librement le flag à tout moment du cycle de vie de l'abonnement. Scoping `ensureBelongsToCurrentEntreprise` (Forbidden si autre entreprise). 2 tests service + 1 controller. **668 verts**.
- [x] **Souscription / upgrade / downgrade Abonnement** (PROPRIETAIRE) (2026-05-17) — `POST /api/v1/abonnements/subscribe` (permission `SUBSCRIPTION_CREATE`). Workflow **2 étapes séparées** : crée Abonnement EN_ATTENTE (sans `dateDebut`/`dateFin`, fixés à l'étape 7 paiement). Décision upgrade/downgrade : **remplacement à dateFin** — l'abonnement actuel reste ACTIF jusqu'à sa fin, le nouveau démarre `currentActif.dateFin+1` (calcul au paiement). DTOs `SubscribeRequest(planId, typeId, couponCode?, renouvellementAuto)` + `SubscribeResponse(abonnement, breakdown, couponCodeApplied, promotionNomApplied)` + `SubscriptionAmountBreakdown(prixDeBase, reductionType, reductionPromotion, reductionCoupon, montantAPayer)` + `AbonnementResponse(Abonnement)`. Calculateur dédié `SubscriptionAmountCalculator` (`@Component`) avec record `SubscriptionAmountInputs` (4 champs ≤ règle params). Applique réductions séquentielles : prix×durée → type → promotion (auto, plan match) → coupon. Clamp à zéro. POURCENTAGE divise par 100, MONTANT_FIXE soustrait direct, scale BigDecimal 2 HALF_UP. Validations : plan `actif && visible && !trial`, type `actif`, coupon `actif + fenêtre temporelle + utilisations restantes + plan match`. Réservation coupon à la souscription (`UtilisationCoupon` créé + `coupon.nombreUtilisations++`). Tests : 6 calculator + 9 service + 3 controller. **665 verts**.
- [x] **Paiement abonnement (manuel + validation admin)** (2026-05-17) — workflow paiement manuel hors intégrateur. `POST /api/v1/paiements-abonnement/abonnements/{id}` multipart (perm `SUBSCRIPTION_PAY`) avec preuve image obligatoire. Montant **recalculé côté serveur** (`SubscriptionAmountCalculator` réutilisé) au moment du paiement. Admin valide (`PATCH /{id}/validate`, perm `SUBSCRIPTION_VALIDATE`) → `Abonnement.statut=ACTIF` + `dateDebut`/`dateFin` calculés (today ou `currentActif.dateFin+1`). Admin rejette (`PATCH /{id}/reject` avec `motifRejet`) → rollback coupon (`UtilisationCoupon` supprimée + `nombreUtilisations--`). Entité `PaiementAbonnement` enrichie : `statut` (enum `StatutPaiementAbonnement{EN_ATTENTE_VALIDATION,VALIDE,REJETE}`), `preuve` (PieceJointe @OneToOne cascade ALL), `motifRejet` (TEXT). Migration V21. DTOs Request/Response/Filter/RejectRequest. Listing paginé `GET /paiements-abonnement` auto-scopé entreprise pour non-ADMIN. Endpoints `GET /{id}` + `GET /{id}/preuve` (download bytes image). Nouvelle permission `SUBSCRIPTION_VALIDATE` (ADMIN). 14 tests service + 5 controller. **687 verts**.
- [!] **Renouvellement automatique** — **DIFFÉRÉ : dépend de l'intégration d'un intégrateur de paiement automatique** (Wave / Orange Money / Stripe / PayPal). Aujourd'hui le paiement est manuel (étape 7 livrée : propriétaire upload preuve + admin valide), donc le concept "auto-débit du propriétaire à `dateFin`" n'a pas de support technique. À reprendre quand l'intégrateur paiement sera mis en place. Le flag `Abonnement.renouvellementAuto`, la permission `SUBSCRIPTION_RENEW`, et l'endpoint PATCH `/{id}/renouvellement-auto` restent en place jusque-là (cleanup éventuel à reporter aussi).
- [x] **Listing abonnements + statut courant entreprise** (2026-05-17) — 3 endpoints : `GET /api/v1/abonnements` (ADMIN, filtre `entrepriseId/statut/planId`), `GET /me` (PROPRIETAIRE auto-scopé entreprise), `GET /me/current` (PROPRIETAIRE → `CurrentAbonnementResponse(abonnement, joursRestants, isTrial, fonctionnalites)`). Records `AbonnementFilter` + `CurrentAbonnementResponse` + `PlanFeaturesResponse(gestionStock/vente/achat/comptabilite, magasinsMax, employesMax)`. Repository `findEntitiesByFilter` JPQL SpEL + mapping en mémoire `page.map(AbonnementResponse::new)`. 4 tests service + 3 controller. **694 verts**.
- [x] **Permissions + catalogue public** (2026-05-17) — permissions `PLAN_*`, `SUBSCRIPTION_TYPE_*`, `COUPON_*`, `PROMOTION_*`, `SUBSCRIPTION_*` + `SUBSCRIPTION_PAY`/`SUBSCRIPTION_RENEW` ajoutées au YAML et à `PermissionCode` (étape 0). Endpoint **agrégé** `GET /api/v1/catalog/public` (permitAll dans `SecurityConfig`) retourne `PublicCatalogResponse(plans, subscriptionTypes, globalPromotions)`. Plans = `actif=true && visible=true` triés par ordre, avec promotions actives imbriquées par planId. Types actifs triés par ordre+durée. Promotions globales (`plan=null`) en section dédiée. Filtre fenêtre temporelle `dateDebut ≤ today ≤ dateFin` côté JPQL. 3 tests service + 2 controller. **646 verts**.
- [x] **Refactor module abonnement vs conventions** (2026-05-17) — 7 blocs : R1 helper `SubscriptionRules` (factorise `ensureReductionConsistent` ×3 + `ensurePeriodValid` ×2 — règle 4, +9 tests), R2 `IPlanAbonnementService.findByIdOrNull(UUID)` default method (règle 27), R3 record `PaiementAbonnementCreationContext` (4 → 1 param, règle 30), R4 injection `ValidatorService` + `validate(filter)` en première ligne sur tous `findAll(filter)`/`findMyHistory(filter)` (règle 33), R5 suppression méthodes privées non-triviales (règles 27 + 4), **R6 setters+save dans les DomainService (règle 26)** : 4 `setActive(entity, boolean)`, `setRenouvellementAuto`, `activate(abonnement, dateDebut, dateFin)`, `incrementUsage`/`decrementUsage`, `UtilisationCouponDomainService.create`, `markAsValide`/`markAsRejete`. **R7 optimisations repository (projection record obligatoire)** : R7a `AbonnementRepository.findLatestActifDateFin(entrepriseId, excludeId) → Optional<LocalDate>` JPQL `MAX(dateFin)` (au lieu de charger entité + filter/map mémoire) + projection `findResponsesByFilter` avec `SELECT new AbonnementResponse(abonnement) ... LEFT JOIN FETCH plan/type/entreprise` + countQuery ; R7b `existsByAbonnementIdAndStatut → boolean` (au lieu de `findFirstBy...isPresent()`) ; R7c `findCouponIdByAbonnementId → Optional<UUID>` + `deleteByAbonnementId` JPQL `@Modifying` bulk (au lieu de charger `UtilisationCoupon` entière + delete) ; R7d suppression `findByNom` orphelins Plan + Type. Cas justifiés conservés en entité (table petite + cohérence) : `findByCode`, `findFirstActivePromotionForPlan`, `findCurrentActif`, `findFirstByTrialTrueAndActifTrue`, `findAllVisibleAndActif`. **703 verts** (+9 nouveaux tests `SubscriptionRulesTest`).
- [ ] Couche `application/` : services use case + DTO + mappers (par module, en commençant par `security`, `magasin`, `users`)
- [ ] Couche `presentation/` : controllers REST (CRUD basique sur chaque ressource)
- [ ] Specifications JPA (`infrastructure/specifications/`) pour les filtres complexes (recherche produits, etc.)
- [ ] Use case stock FIFO : `EntreeStock` consommée par `SortieStock` lors d'une vente
- [ ] Génération automatique des références (`commande_vente.reference`, `facture_*.numero`)
- [ ] Notifications : worker qui parcourt `Echeance` et envoie via canal `EMAIL` / `SMS`
- [ ] **Compléter la config Actuator** — élargir endpoints exposés (`health, info, metrics`), `/actuator/health` permitAll dans `SecurityConfig` pour health checks Docker/K8s, `show-details: when-authorized`, `management.info.*`. Optionnel : `micrometer-registry-prometheus` si scraping Grafana.

### Frontend
- [ ] Pages CRUD de base (produits, catégories, qualité) : table + form modal
- [ ] Dashboard accueil (KPIs simples : ventes du jour, stock bas, échéances à venir)
- [ ] Cycle vente UI : sélection client → ajout produits → paiement → impression facture
- [ ] Cycle achat UI : sélection fournisseur → commande → réception → facture
- [ ] Sélecteur de magasin (si propriétaire en a plusieurs) → propage dans store Zustand
- [ ] Gestion erreurs API globale (toast + retry) via React Query

---

## 🟢 Priorité basse / Nice-to-have

### Backend
- [ ] Tests d'intégration sur les flux critiques (inscription, vente, paiement)
- [ ] Dashboard / endpoints de reporting (CA, marge, stock dormant)
- [ ] OpenAPI groupé par module + auth bearer dans Swagger UI
- [ ] Dockerfile + docker-compose (app + Postgres)
- [ ] Documentation API enrichie
- [x] **Intégration SonarQube + JaCoCo** (2026-05-18) — Self-hosted via `docker-compose.sonar.yml` (SonarQube LTS Community + PostgreSQL 16). Config Sonar **externalisée** dans `sonar-project.properties` à la racine (format officiel Sonar, key=value — pas YAML car non supporté nativement). `pom.xml` contient uniquement les **versions** des plugins : `jacoco-maven-plugin` 0.8.13 (prepare-agent + report), `properties-maven-plugin` 1.2.1 (charge `sonar-project.properties` en phase `initialize`), `sonar-maven-plugin` 5.0.0.4389. Le fichier `.properties` est lu nativement par le scanner CLI ET par `mvn sonar:sonar` via `properties-maven-plugin`. Guide complet dans `docs/SONAR.md` : setup admin + token, scan `mvn clean verify sonar:sonar`, lecture rapport JaCoCo HTML local, Quality Gate, troubleshooting. Aucune modif de code ni de test ; coverage runtime via agent JVM.

### Frontend
- [ ] Génération auto des types DTO depuis l'OpenAPI du backend (`openapi-typescript`)
- [ ] i18n (next-intl ou équivalent) — au minimum FR ; EN plus tard
- [ ] Dark mode toggle (les CSS variables shadcn le supportent déjà)
- [ ] PWA / mode offline minimal
- [ ] Tests Vitest + Testing Library

---

## ✅ Terminées

- [x] **Analyse initiale du projet** — cartographie des 14 modules, ~56 entités, identification des incohérences
- [x] **Refactor Lombok** — remplacement des getters/setters manuels par `@Getter`/`@Setter` sur toutes les entités
- [x] **Ajout dépendance Lombok** dans `pom.xml` + exclusion dans plugin Spring Boot
- [x] **Création des JPA repositories** (35 fichiers) — `<Entity>JpaRepository extends JpaRepository<Entity, UUID>` dans `<module>/domain/repository/`
- [x] **`GlobalService<E, R>`** — classe abstraite générique CRUD dans `common/service`
- [x] **`ValidatorService`** — wrapper `jakarta.validation.Validator` dans `common/service`
- [x] **Création des Domain Services** (35 fichiers) — `<Entity>DomainService extends GlobalService<...>` dans `<module>/domain/service/`
- [x] **Remplissage README.md** — présentation, stack, architecture, modèle de données
- [x] **Chaîne JWT complète** — `JwtService` (generate/validate/extract), `JwtAuthenticationFilter` (Bearer → `UserPrincipal` dans `SecurityContext`), `UserDetailsServiceImpl` (lookup `Account` + authorities depuis `Role.permissions.code`)
- [x] **Externalisation config JWT** — record `org.store.property.JwtProperties` (`security.jwt` : secret, header, prefix, expiration access/refresh) + `@ConfigurationPropertiesScan` sur `StoreApplication`
- [x] **Enum `Claim`** — clés JWT centralisées (`ENTREPRISE`, `MAGASIN`, `USERNAME`, `PERMISSIONS`)
- [x] **Nettoyage `SecurityConfig`** — suppression des doubles bindings (`@Component`/`@Bean` sur `JwtAuthenticationFilter` et `@Service`/`@Bean` sur `UserDetailsServiceImpl`)
- [x] **Renommage `UserPrincipal.email` → `username`** — cohérence avec `Account.username`
- [x] **Activation `@EnableJpaAuditing`** sur `StoreApplication` (⚠️ `AuditorAware` reste à brancher)
- [x] **`AuditorAwareImpl`** (`org.store.config`) — lit le `UserPrincipal` du `SecurityContext` et renvoie `userId.toString()` ; `Optional.empty()` si pas d'authentification
- [x] **Refactoring hexagonal pragmatique** — extraction des ports `<Entity>Repository` (interface pure dans `domain/repository/`) et adapters `<Entity>JpaRepository` (Spring Data dans `infrastructure/repository/`) pour les 35 entités. `BaseRepository<E>` dans `common/repository/`. `GlobalService` typé sur le port. Faute `repositorty/` corrigée.
- [x] **i18n backend FR/EN** — `I18nConfig` (`MessageSource` UTF-8, `AcceptHeaderLocaleResolver` default FR + supported [FR, EN], `LocalValidatorFactoryBean` câblé), `IMessageSourceService` + `MessageSourceServiceImpl`, `LocalizedRuntimeException` parente des 12 exceptions custom (porte `messageKey` + `args`), `GlobalException` advice résout via `IMessageSourceService`, `messages.properties` / `messages_en.properties` (clés applicatives + 22 clés Bean Validation Jakarta)
- [x] **`DataInitializer`** (`org.store.config`, `ApplicationRunner`) — seeds idempotents au boot : `Permissions` (`PROPRIETAIRE_ACCESS`, `EMPLOYE_ACCESS`), `Role` (`PROPRIETAIRE`, `EMPLOYE`), `PlanAbonnement` trial gratuit (1 magasin, 3 employés, 30 jours)
- [x] **Endpoints `/api/v1/auth/*`** — `POST /register` (201 + tokens), `POST /login` (200 + tokens), `POST /refresh` (200 + nouveau access), `POST /logout` (204, idempotent). `RefreshToken` UUID opaque persisté (7 jours), pas de rotation. `AuthController` + 11 services applicatifs (`IAccountService`, `IProprietaireService`, `IEntrepriseService`, `IMagasinService`, `IAbonnementService`, `ILoginService`, `IRefreshTokenService`, `IRegisterPropertyService`, `IUserPrincipalFactory`, `IJwtService`)
- [x] **Module `entreprise`** extrait du module `magasin` — `Entreprise` (entité, port, adapter, services, DTO, test) déplacés dans `org.store.entreprise.*`. Imports cross-modules ajustés.
- [x] **Tests unitaires + slice web** — 37 tests passants (services + `AuthController` en `MockMvcBuilders.standaloneSetup`, Spring Boot 4 ayant retiré `@WebMvcTest`/`@MockBean` du starter de base)
- [x] **Flyway** (2026-05-12) — `spring-boot-starter-flyway` 4.0.6 + `flyway-database-postgresql`, `V1__init_schema.sql` manuel (~485 lignes, 40 tables, héritage JOINED `person`/`utilisateur`, table jointe `role_permission`, FK via `ALTER TABLE` à la fin), `ddl-auto: validate`, `baseline-on-migrate=true`, `baseline-version=1`
- [x] **Validators custom** (`common/validation/`) — `@Phone` (regex SN), `@EnumValue` (vérif contre une classe enum), `@DatePattern` (parseable via `DateTimeFormatter`), `@Uuid` (parseable via `UUID.fromString`). Chacun avec test paramétré + clé i18n
- [x] **Téléphone obligatoire** dans la registration propriétaire (`UtilisateurRequest.telephone` `@NotBlank @Phone`)
- [x] **Service isolation** — tous les `<X>ServiceImpl` injectent désormais leur `<X>DomainService` (jamais un `<X>Repository` direct). DomainServices enrichis de méthodes custom (`findByUsername`, `findByToken`, `findByLibelle`, `findFirstTrialActif`, `findAllByRoleId`). `UserDetailsServiceImpl` aussi
- [x] **Enum `PermissionCode`** — codes permissions centralisés (`PROPRIETAIRE_ACCESS`, `EMPLOYE_ACCESS`, `EMPLOYE_CREATE`). Overload `UserPrincipal.hasPermission(PermissionCode)`. `DataInitializer` et `EmployeServiceImpl` utilisent l'enum
- [x] **`UserPrincipal.role`** — libellé du rôle ajouté au record + claim JWT `ROLE("role")` dans l'enum `Claim`
- [x] **`ICurrentUserService`** — récupère le `UserPrincipal` depuis `SecurityContextHolder` ; les services métier l'injectent au lieu d'utiliser `@AuthenticationPrincipal` au controller
- [x] **`PermissionsDomainService.findAllByRoleId`** — JPQL projection sur `role_permission`, retourne `List<String>` de codes. Évite l'accès LAZY. Utilisé par `UserPrincipalFactoryImpl`, `UserDetailsServiceImpl`, `EmployeServiceImpl`
- [x] **`EmployeResponse(Employe)`** — constructeur secondaire qui extrait les champs depuis l'entité (convention applicable à tous les `<X>Response`)
- [x] **`FONCTIONNALITIES.md`** à la racine du backend — récap des 5 services applicatifs métier (auth × 4 + employé)

---

## 🐛 Erreurs connues / dette

### Backend
- [ ] `org.store.common.tools` — classe vide, nom en minuscule (à supprimer ou renommer)
- [ ] `Entreprise.proprietaire nullable=false` + héritage `Utilisateur` : chaîne d'inscription à clarifier (poule/œuf entre `Account` et `Proprietaire`)
- [ ] `application.yml` contient des credentials par défaut hardcodés → à externaliser

### Frontend
- [ ] `package.json` : entrée `"root": "github:tanstack/react-query"` non standard — devrait être `"@tanstack/react-query": "..."`. À corriger ou vérifier que l'install fonctionne.
- [ ] `src/app/layout.tsx` : metadata `Create Next App` à remplacer ; `lang="en"` → `lang="fr"`.
- [ ] `src/app/page.tsx` : encore le template Next.js par défaut.
- [ ] `README.md` du frontend : encore le README générique de `create-next-app`.

---

## 🗒️ Notes & décisions

- **2026-05-09** — `JpaRepository` placés dans `domain/repository/` plutôt que `infrastructure/`, choix pragmatique vs hexagonal strict.
- **2026-05-09** — Tous les services domaine héritent de `GlobalService<E, R>` qui fournit le CRUD de base. Logique métier spécifique à ajouter au cas par cas.
- **2026-05-09** — Lombok `@Getter`/`@Setter` uniquement, jamais `@Data` ni `@ToString` (sécurité JPA).
- **2026-05-09** — Harmonisation FR des noms de tables (`product` → `produit`, etc.) **annulée** : on garde les noms d'origine.
- **2026-05-09** — Custom `EntityException` (dans `common/exceptions`) utilisée à la place de `jakarta.persistence.EntityNotFoundException`.
- **2026-05-09** — Mono‑repo confirmé : ajout du frontend `store-frontend/` (Next.js 16 + shadcn + base‑ui).
- **2026-05-09** — Frontend stack figée : Tailwind 4, shadcn `base-nova`, react-hook-form + zod, axios + React Query, zustand.
- **⚠️ Next.js 16** — version récente avec breaking changes ; toujours consulter `store-frontend/AGENTS.md` et `node_modules/next/dist/docs/` avant d'écrire du code Next.
- **2026-05-10** — Config JWT externalisée dans `application.yml` sous `security.jwt`, bindée via record `JwtProperties` (package `org.store.property`).
- **2026-05-10** — Clés de claims JWT centralisées dans l'enum `org.store.security.application.enums.Claim` — toujours utiliser `Claim.X.getKey()`, jamais de string littérale.
- **2026-05-10** — `JwtAuthenticationFilter` ne re-valide PAS le statut du compte (`enabled`/`locked`) à chaque requête — il fait confiance au JWT signé. À rebrancher si besoin de durcir.
- **2026-05-10** — `UserDetailsServiceImpl` retourne un `User` Spring (pas de wrapper custom autour d'`Account`). Conséquence : le service de login devra refaire un `findByUsername` pour construire le `UserPrincipal` du JWT.
- **2026-05-10** — `UserPrincipal.email` renommé en `username` pour rester cohérent avec `Account.username`.
- **2026-05-11** — Pas d'entité `Administrator` dédiée : un admin SaaS = `Utilisateur` racine (sans sous-classe `Proprietaire`/`Employe`) + `Account` avec un `Role` "ADMIN". Distinction au runtime via `Account.role`. Si besoin futur de champs propres aux admins → créer `Administrator extends Utilisateur` à ce moment-là.
- **2026-05-11** — **Refactoring hexagonal pragmatique** : décision précédente (2026-05-09) sur `JpaRepository` dans `domain/repository/` **annulée**. Désormais : port `<Entity>Repository extends BaseRepository<E>` dans `domain/repository/` (interface pure), adapter `<Entity>JpaRepository extends JpaRepository, <Entity>Repository` dans `infrastructure/repository/`. `GlobalService` typé sur `<Entity>Repository`. Les entités JPA restent dans `domain/model/` (compromis assumé). Faute de frappe `repositorty/` corrigée partout.
- **2026-05-11** — **i18n backend FR/EN** mis en place : default `Locale.FRENCH`, locale résolue via header `Accept-Language` (`AcceptHeaderLocaleResolver`). Toutes les exceptions custom (sauf `AuthentificationException` qui hérite de Spring Security) héritent désormais de `LocalizedRuntimeException` et portent `(messageKey, args)`. L'advice `GlobalException` résout via `IMessageSourceService`. Clés Bean Validation Jakarta overridées dans `messages*.properties`. `useCodeAsDefaultMessage=true` → si une clé est absente, le code est retourné tel quel (fallback safe). Convention : préfixer les clés par domaine (`entity.*`, `validation.*`, `error.*`, `auth.*`, etc.).
- **2026-05-11** — **Règles obligatoires posées** (toutes inscrites en mémoire user) :
  - Controller : `public static final String BASE_PATH = "/api/v1/<scope>"` (version hardcodée) + `@RequestMapping(BASE_PATH)`, pas de `@PathVariable("version")` sur les handlers
  - Chaque service applicatif a **obligatoirement** une interface `I<X>Service` + impl `<X>ServiceImpl` ; sites d'injection utilisent l'interface
  - DTOs : suffixe `<X>Request` (entrée) ou `<X>Response` (sortie), jamais `Info` ni `Dto` générique
  - Paramètre de méthode de type DTO : nom = camelCase complet du type (`AccountRequest accountRequest`), pas `info`/`request`/`dto` génériques
  - Tests : chaque service applicatif a un `<X>ImplTest` (JUnit5 + Mockito), chaque controller un `<X>ControllerTest` (`@WebMvcTest` ou `MockMvcBuilders.standaloneSetup`)
- **2026-05-11** — **Module `entreprise`** : sous-domaine `Entreprise` extrait du module `magasin` pour cohérence métier (l'entreprise est un agrégat racine multi-tenant, distinct du magasin). Le module `magasin` ne porte plus que `Magasin`. Imports croisés ajoutés (`Magasin.entreprise`, `Entreprise.magasins`).
- **2026-05-11** — **Refresh token** : UUID opaque persisté (table `refresh_token`, durée `properties.expiration().refreshToken()` = 7j). **Pas de rotation** pour MVP. `/auth/logout` révoque (idempotent). L'access token JWT ne peut pas être révoqué côté serveur (stateless) — il expire seul en 1h.
- **2026-05-11** — **Spring Boot 4 — packages test restructurés** : `@WebMvcTest` n'est plus dans `spring-boot-starter-test`, il faut `spring-boot-starter-webmvc-test`. `@MockBean` supprimé → remplacé par `@MockitoBean` (`org.springframework.test.context.bean.override.mockito`). Solution actuelle : `MockMvcBuilders.standaloneSetup()` pour les tests controller (aucune dépendance pom supplémentaire).
- **2026-05-12** — **Flyway / Spring Boot 4** : l'autoconfig Flyway est sortie de `spring-boot-autoconfigure`. Il faut `spring-boot-starter-flyway` (module dédié SB 4) + `flyway-database-postgresql`. `baseline-version: 1` pour que les BDD existantes (déjà peuplées par `ddl-auto: update`) considèrent V1 comme déjà appliquée. `V1__init_schema.sql` rédigé à la main (40 tables, JOINED `person`/`utilisateur`, `role_permission`, FK via `ALTER TABLE` à la fin).
- **2026-05-12** — **Service isolation renforcée** : tous les `<X>ServiceImpl` injectent leur `<X>DomainService`, jamais un `<X>Repository`. Les DomainServices exposent les queries custom (`findByUsername`, `findByToken`, `findByLibelle`, `findFirstTrialActif`, `findAllByRoleId`) — renvoient `Optional<E>`, l'app service throw. `UserDetailsServiceImpl` (Spring Security) suit la même règle.
- **2026-05-12** — **Validators custom** (`org.store.common.validation`) : `@Phone` (regex SN `^(70|75|76|77|78|33)\d{7,9}$`), `@EnumValue(enumClass, ignoreCase)`, `@DatePattern(pattern)` (SMART mode), `@Uuid`. Tous skippent null/empty (combiner avec `@NotBlank` si requis). Chaque validateur a sa clé i18n + test paramétré.
- **2026-05-12** — **`PermissionCode` enum** : codes permissions centralisés (`PROPRIETAIRE_ACCESS`, `EMPLOYE_ACCESS`, `EMPLOYE_CREATE`). Le `code` en BDD = `enum.name()`. Overload `UserPrincipal.hasPermission(PermissionCode)`. Plus aucune chaîne de permission éparpillée.
- **2026-05-12** — **`UserPrincipal.role`** : champ String ajouté (libellé du rôle, pas UUID). Sérialisé dans le JWT via `Claim.ROLE("role")`. `UserPrincipalFactoryImpl` utilise `account.role.libelle`. (Décision : libellé > UUID pour la flexibilité d'ajout de rôles.)
- **2026-05-12** — **`ICurrentUserService`** : remplace `@AuthenticationPrincipal UserPrincipal` au controller. Les services métier appellent `currentUserService.getCurrent()` → testable avec un simple mock (vs setup SecurityContext + custom argument resolver dans MockMvc).
- **2026-05-12** — **Règles employé data-driven** : aucun libellé `"MANAGER"` hardcodé dans `EmployeServiceImpl`. La règle "rôle élevé = peut créer des employés" se base sur la permission `EMPLOYE_CREATE`. Ajouter un futur `CAISSIER` (avec `EMPLOYE_ACCESS`) ou `SUPER_MANAGER` (avec `EMPLOYE_CREATE`) ne demande **aucun changement applicatif** — seulement un `ensureRole(...)` dans `DataInitializer`.
- **2026-05-12** — **`<X>Response(<X> entity)`** : tout DTO de sortie doit exposer un constructeur secondaire qui extrait les champs depuis l'entité. Centralise le mapping, supprime la duplication dans les services.
- **2026-05-12** — **`SELECT new <X>Response(entity)` dans `@Query`** : projection JPQL via le constructeur secondaire de Response, jamais la liste des champs en clair (DRY, pas de désync sur ajout de champ). Pré-requis : la convention `Response(Entity)` doit être appliquée.
- **2026-05-12** — **`create()` dans le DomainService** : construction d'entité (`new` + setters + `save`) vit dans `<X>DomainService.create(...)`. L'app service délègue + applique les règles métier. Sépare clairement "persistance" (domain) de "orchestration" (application).
- **2026-05-12** — **Réorganisation packages `impl`** : toutes les classes `<X>ServiceImpl` déplacées dans `<module>/application/service/impl/`. Cohérence transverse (interface dans `service/`, impl dans `service/impl/`).
- **2026-05-12** — **Pas de méthodes privées dans les services applicatifs** : toute factorisation devient une méthode publique de `I<X>Service`, paramétrée plutôt que spécialisée (ex. `createAccount(request, String roleName)` vs `doCreate(request)` qui hardcodait `ROLE_PROPRIETAIRE`). Exceptions tolérées : helpers triviaux sur DTO/utilitaire, pas un service métier.
- **2026-05-12** — **Pas de logique métier dans le controller** : handler = `return service.method(request)`. Pas de cast, pas de `new Response(...)`, pas de getter-chain, pas d'orchestration multi-services. Si la méthode métier nécessaire crée un cycle de DI, la créer dans le service qui orchestre déjà (vu : `IRegisterPropertyService.registerEntrepriseByAdmin` plutôt que `IEntrepriseService.createByAdmin`).
- **2026-05-12** — **Sous-DTO Response pour sous-entité** : quand un `<X>Response` agrège ≥ 3 champs d'une autre entité réutilisable, créer un `<Y>Response` dédié (ex. `AccountResponse.user: UserResponse`) plutôt que de mettre les champs à plat. JSON hiérarchique, DRY, suit la convention `Response(Entity)`.
- **2026-05-12** — **Strategy pattern pour dispatch par sous-type** : éviter `if (x instanceof A) ... else if (x instanceof B)` dans le code de dispatch. Pattern : interface `<X>Strategy { Class<? extends Parent> targetType(); <Y> resolve(Parent x); }` + impls `@Component` + Spring injecte `List<Strategy>` + dispatch via `.filter(s -> s.targetType().isInstance(x)).reduce((a,b) -> a.targetType().isAssignableFrom(b.targetType()) ? b : a)` → "most-specific wins". Permet fallback sur le type parent.
- **2026-05-12** — **Seed ERP additif** : les 4 anciennes permissions (PROPRIETAIRE_ACCESS, EMPLOYE_ACCESS, EMPLOYE_CREATE, ADMIN_ACCESS) conservées en BD pour que le code Java actuel continue de fonctionner. Les 75 nouvelles (MODULE_ACTION) cohabitent. Migration progressive des `@PreAuthorize` à faire plus tard.
- **2026-05-12** — **`Proprietaire` n'a pas de `magasinId`** dans `UserPrincipal` : un OWNER possède tous les magasins de son entreprise, pas un magasin particulier. Le `magasinId` n'est porté que par les `Employe` (Manager/Vendeur).
- **2026-05-12** — **DomainService = port d'accès aux données** : tout CRUD ou query custom est dans `<X>DomainService`. L'application ne voit jamais `<X>Repository`. Une méthode réutilisable (ex. `extractPermissionCodes`) doit vivre dans le service propriétaire de l'agrégat, jamais en helper privé dupliqué entre services.
